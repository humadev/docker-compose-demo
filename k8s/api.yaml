apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  labels:
    app: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: demo-api
        image: demo-api:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 4000
        env:
        - name: MYSQL_DATABASE
          value: 'demo-db'
        - name: DB_URL
          valueFrom:
            configMapKeyRef:
              name: mysql-config
              key: db-url
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-user
              key: db-url
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-password
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: NodePort
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
      nodePort: 30000